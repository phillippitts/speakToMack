# Production profile overrides for Actuator exposure
# Limit public endpoints to health and info; hide health details
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never
